# PhoneBridge 更新记录

## 0.15.9（2026-08-31）

### 调整

- 文件列表取消“名称”和“大小”之间的无限伸展空白，名称、大小、日期改为紧凑对齐列；空余宽度放到日期列之后，不再形成列表中央的大块无效区域。
- Mac 文件名列和手机文件名列分别按内容密度压缩，长文件名自动省略，鼠标悬停可查看完整名称。
- 优化多设备宽度分配：限制 Mac 与非末位手机面板的无效扩张，让最右侧设备获得更合理的可视宽度，同时保留独立横向滚动和可拖动分隔线。
- 应用版本更新为 `0.15.9 (Build 36)`。

## 0.15.8（2026-08-31）

### 调整

- 移除 Mac 与全部手机文件面板共用的工作区横向滚动条，改为 Mac、每台 iPhone/Android 各自拥有独立横向滚动条；滚动某一栏时不再移动其他设备栏。
- 面板间继续使用可拖动分隔线调整宽度；当某个面板宽度不足时，只在该面板内部横向查看完整路径、工具栏和文件列。
- 应用版本更新为 `0.15.8 (Build 35)`。

## 0.15.7（2026-08-31）

### 调整

- 多面板工作区改为原生横向布局，保留横向滚动，并支持拖动分隔条调整每个文件面板的宽度。
- 应用版本更新为 `0.15.7 (Build 34)`。

### 修复

- 修复 0.15.6 引入横向滚动后，`HSplitView` 在部分窗口尺寸下把各面板顶部工具栏裁出可视区域，导致 Mac 标题以及手机“投屏 / 传输到手机 / 文本 / 刷新文件”按钮全部消失的问题。

## 0.15.6（2026-08-31）

### 调整

- iPhone AirPlay 改为全局单接收器：启动另一台 iPhone 投屏前会先完整停止旧接收器，同一台 Mac 只广播一个 PhoneBridge，避免手机“屏幕镜像”列表出现多个同名项目。
- 普通局域网模式恢复 UxPlay 官方的全可用网卡 Bonjour 注册，兼容 Mac 通过有线、USB 网卡或 Wi-Fi 接入局域网；重复名称改由全局单进程和稳定 Device ID 解决。
- 文件工作区增加横向滚动条；窗口宽度不足以容纳 Mac、多手机和投屏面板时保持各面板最小宽度，可横向滑动查看完整内容。
- AirPlay Device ID 改为应用级稳定值，并将应用版本更新为 `0.15.6 (Build 33)`。

### 修复

- 修复 Mac 进程较多时，启动扫描使用 `waitUntilExit` 等待 `/bin/ps`、而 `/bin/ps` 又因输出管道写满无法退出，导致应用启动阶段主线程死锁、Dock 图标持续跳动的问题。
- 修复 Mac 的默认局域网实际走有线或 USB 网卡时，AirPlay 被固定发布到 Wi-Fi 接口，导致 iPhone“屏幕镜像”完全找不到 PhoneBridge 的问题。
- iPhone 已收到有效投屏画面后断流时，PhoneBridge 会立即结束对应 UxPlay；同时识别 UxPlay 的 RTP shutdown、socket closed 和 video stop 日志作为兜底。
- UxPlay 停止流程增加 `SIGINT → SIGTERM → SIGKILL` 分级回收，应用退出时同步清理子进程，避免 UxPlay 成为由 PID 1 接管的残留进程。
- 应用启动时只查找并清理当前 PhoneBridge 包内的遗留 UxPlay，不影响用户从 Homebrew 或终端单独启动的 UxPlay。

## 0.15.5（2026-08-28）

### 调整

- 扫码配对成功后，如果 `_adb-tls-connect._tcp` 广播仍被跨网段阻断，PhoneBridge 会经 USB 读取手机对外监听的高位端口，并用已配对密钥逐个验证无线 ADB 连接；短暂失败的端口会自动重试。
- 应用版本更新为 `0.15.5 (Build 32)`。

### 修复

- 修复跨网段 USB 辅助扫码已经完成安全配对，但第二阶段无法发现无线连接端口、界面长期停留在“正在自动建立无线连接”的问题。
- 修复 `adb connect` 即使输出 `failed to connect` 仍可能返回退出码 0，导致应用把失败命令当成成功的问题；现在同时校验命令输出。

## 0.15.4（2026-08-28）

### 调整

- 手机与 Mac 位于不同子网、mDNS 广播无法跨网段时，如果 Android 仍通过数据线连接，PhoneBridge 会经 USB 读取本次二维码对应的动态配对端口和 Wi-Fi 地址，再通过可路由的单播完成加密配对与自动连接。
- 应用版本更新为 `0.15.4 (Build 31)`。

### 修复

- 修复公司网络把 Mac 与手机分配到不同子网时，手机已正确发布二维码服务但 mDNS 无法跨网段到达 Mac、导致双方持续等待的问题；有线设备现在可自动走 USB 辅助发现。

## 0.15.3（2026-08-28）

### 调整

- Android 扫码配对前显式串行启动 ADB，并在界面展示“启动 ADB / 等待扫码 / 等待手机发布服务 / 安全配对 / 自动连接”等具体阶段和等待秒数。
- 应用版本更新为 `0.15.3 (Build 30)`。

### 修复

- Android 二维码配对新增 macOS 原生 Bonjour 发现兜底，并与 `adb mdns services` 的结果合并，修复部分 Mac 上手机扫码后一直加载、PhoneBridge 无法发现 `_adb-tls-pairing._tcp` 服务的问题。
- Bonjour 搜索失败或等待超时时给出本地网络权限、同网段、访客网络隔离、手机热点和手动配对等可操作提示，不再只停留在无限加载状态。

## 0.15.2（2026-08-28）

### 新增

- Android 无线调试新增系统二维码配对：PhoneBridge 生成 Android Studio 兼容的一次性二维码，手机通过“使用二维码配对设备”扫描即可开始配对。
- 扫码后自动通过 ADB mDNS 匹配手机广播的配对服务，完成安全配对并发现最新连接端口，无需手动输入配对地址、6 位配对码和连接地址。
- 保留“手动配对与连接”折叠区，作为公司网络屏蔽 mDNS 或部分机型扫码不可用时的备用入口。

### 调整

- 二维码使用随机 `studio-*` 服务名和 12 位一次性密码；关闭无线连接窗口、点击停止或重新生成时，旧配对流程立即停止。
- 增加 Android ADB 配对/连接 Bonjour 服务声明和本地网络用途说明。
- 应用版本更新为 `0.15.2 (Build 29)`。

## 0.15.1（2026-08-28）

### 调整

- 点击 Mac 或手机文件面板的“名称 / 大小 / 日期”表头时，先重新读取对应目录，再按所选字段和方向排序。
- 同一台 Android 同时存在 USB 与无线 ADB 通道时，按硬件序列号合并为一个设备，并优先使用更稳定的 USB 通道；不同实体手机不受影响。
- 录屏保存窗口改为附着在 PhoneBridge 主窗口上显示；取消保存后在原窗口二次确认，可返回继续保存或明确放弃录像。
- 应用版本更新为 `0.15.1 (Build 28)`。

### 修复

- 修复排序时仍使用刷新前文件数据，导致新文件未及时参与排序的问题。
- 修复同一台 Android 同时开启无线调试并插入数据线时出现重复设备面板、可重复打开投屏窗口的问题。
- 修复取消录屏保存后仅保留临时文件、缺少明确放弃入口的问题；确认放弃后删除临时录像，删除失败则继续保留并提示错误。

## 0.15.0（2026-08-28）

### 新增

- 支持多台 Android 与 iPhone 同时投屏：Android 每台设备使用独立 scrcpy 进程；iPhone 每台设备使用独立 UxPlay 进程、JPEG 接收端口和原生窗口。
- 多台 iPhone 同时投屏时，AirPlay 接收名称自动附加设备名；同名设备再附加序号，并为每路 UxPlay 分配稳定且不同的 AirPlay Device ID。
- iOS 媒体日期增加多级兜底：依次读取文件、EXIF 和通用创建/修改日期，并尝试从常见文件名解析时间。

### 调整

- 投屏侧栏只负责查看和控制当前选中的手机；切换设备或收起侧栏不再停止其他设备的投屏。
- iOS 所有日期字段都缺失时，使用 PTP 对象顺序或媒体目录顺序完成稳定的日期升降序排序，但不会在界面伪造日期。
- 每台 iPhone 独立记忆投屏窗口的位置和尺寸。
- 主界面底部“无线连接”和“无线传输”按钮改为蓝色高亮背景，提升入口辨识度。
- 应用版本更新为 `0.15.0 (Build 27)`。

### 修复

- 修复投屏状态由单一“当前设备”持有，导致启动第二台手机时第一台被替换或停止的问题。
- 修复部分 Mac 上 ImageCaptureCore 未返回 `fileCreationDate` 时，iOS 文件日期显示为空且排序结果不稳定的问题。

## 0.14.2（2026-08-27）

### 新增

- 新增 GitHub Actions 自动发布流水线：推送 `main` 后执行 Release 编译，新版本使用 UxPlay v1.73.6 和 scrcpy v4.1 自动生成 Apple Silicon DMG、创建标签并发布到 GitHub Releases。
- 支持手动强制重建并替换同版本 Release 安装包。

### 调整

- Android 投屏固定使用 scrcpy 独立窗口，保留低延迟、键鼠控制、剪贴板和拖放能力。
- 移除 Android 内嵌投屏入口、ScreenCaptureKit 内嵌采集服务和自动重连逻辑，避免额外投屏窗口及内嵌不稳定问题。
- 应用版本更新为 `0.14.2 (Build 26)`，同步更新 README、使用说明、技术方案和 DMG 安装说明。
- 打包脚本改为动态读取 Homebrew 安装前缀和许可证路径，支持本机与 GitHub ARM64 runner 共用。

## 0.14.1（2026-08-27）

### 新增

- iPhone 独立投屏改由 PhoneBridge 原生窗口显示，并提供“显示独立窗口”按钮，窗口被手动关闭后可以重新打开。
- 录屏保存支持自定义文件名；停止录制后可在“另存为”窗口中同时修改名称和选择保存目录。
- 取消录屏保存时保留待保存文件，可通过“保存录像”再次命名并保存。

### 调整

- iPhone 内嵌与独立窗口统一使用稳定的 JPEG/TCP 投屏通道；独立模式不再调用不稳定的 GStreamer Apple 视频输出组件。
- 应用版本更新为 `0.14.1 (Build 25)`。

### 修复

- 修复 iPhone 独立投屏连接后没有画面，以及 UxPlay 因 `avsamplebufferlayersink` 崩溃并返回退出码 11 的问题。
- 修复 iPhone 独立投屏窗口历史尺寸异常时窗口过小的问题。

## 0.14.0（2026-08-27）

### 新增

- 投屏工具栏新增 PNG 截屏和 H.264 MP4 录屏。
- 截图和录像默认打开上次使用的保存目录。
- 支持录屏结束后的待保存状态，避免取消选择目录后丢失本次录像。

### 调整

- 投屏录制最长边限制为 1920 像素，并保持原始画面比例。
- 应用版本更新为 `0.14.0 (Build 24)`。
