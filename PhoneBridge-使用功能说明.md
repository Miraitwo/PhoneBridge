# PhoneBridge 使用与功能说明

> 适用版本：PhoneBridge 0.15.0（Build 27）
> 适用设备：Apple Silicon Mac、Android 手机、iPhone / iPad
> 文档定位：面向安装、使用、演示和问题排查

## 1. 软件简介

PhoneBridge 是一款运行在 macOS 上的个人文件传输与手机投屏工具，主要解决以下场景：

- 使用数据线把 Android 或 iPhone 中的图片、视频传到 Mac。
- 在一个窗口中同时查看 Mac 文件和多台手机的媒体文件。
- 通过勾选、拖拽和同名文件策略，批量完成文件传输。
- 把 Mac 文件拖到 Android 当前目录，或选择具体手机目标文件夹。
- 把 Mac 文件通过一次性下载页发送到 iPhone，并由用户选择 iOS 保存位置。
- 把 Mac 上的网站链接或文本发送到手机剪贴板或系统分享菜单。
- 通过二维码把 Charles CA 证书发送到 iPhone。
- 在同一网络中通过浏览器把手机文件无线上传到 Mac。
- 使用 scrcpy 独立窗口投屏 Android，或使用 UxPlay/AirPlay 以内嵌/独立窗口显示 iPhone 画面。
- 同时连接并投屏多台 Android/iPhone，按设备独立启动、停止和查看画面。

PhoneBridge 0.15.0 的主界面由以下区域组成：

1. 左侧：Mac 文件区。
2. 中间：一台或多台手机文件面板。
3. 最右侧：按需展开的投屏侧栏，启动时默认隐藏。
4. 底部：传输任务、状态、无线连接和无线传输入口。

## 2. 系统要求

### 2.1 Mac

- Apple Silicon Mac：M1、M2、M3、M4 或后续 ARM64 机型。
- macOS 13 或更高版本。
- 首次启动需要允许“本地网络”访问，供 AirPlay 和无线传输使用。

当前发布包已经内置：

- ADB
- scrcpy 与 scrcpy-server
- UxPlay
- GStreamer 运行库与所需插件

安装 DMG 的普通使用者不需要安装 Homebrew。

### 2.2 Android

USB 模式需要：

- 开启“开发者选项”。
- 开启“USB 调试”。
- 数据线连接 Mac 后，在手机上允许这台 Mac 进行调试。

无线 ADB 模式需要：

- Android 11 或更高版本。
- 手机和 Mac 位于同一个局域网或热点中。
- 在“开发者选项 → 无线调试”中获取配对地址、6 位配对码和连接地址。

### 2.3 iPhone / iPad

读取照片和视频需要：

- 使用数据线连接 Mac。
- 保持设备解锁。
- 第一次连接时选择“信任此电脑”。

iPhone 投屏需要以下任一条件：

- 普通模式：Mac 与 iPhone 在同一个可互相发现的局域网。
- 附近模式：Mac 与 iPhone 的 Wi-Fi、蓝牙保持开启，使用点对点 AirPlay/AWDL。

附近模式不依赖共同的路由器，但也不是纯 USB 投屏。

## 3. 安装与首次启动

### 3.1 安装

1. 双击 `PhoneBridge-0.15.0-AppleSilicon.dmg`。
2. 将 `PhoneBridge` 拖到“应用程序”文件夹。
3. 从“应用程序”文件夹启动 PhoneBridge。

### 3.2 macOS 提示无法验证开发者

当前 DMG 使用临时签名，未经过 Apple 公证。如果首次启动被拦截：

1. 在“应用程序”中右键 PhoneBridge。
2. 选择“打开”。
3. 在确认窗口中再次点击“打开”。

如果仍被拦截：

1. 打开“系统设置 → 隐私与安全性”。
2. 找到 PhoneBridge 的安全提示。
3. 点击“仍要打开”。

### 3.3 本地网络权限

看到“是否允许访问本地网络”时请选择“允许”。该权限用于：

- iPhone AirPlay 投屏发现与连接。
- 手机浏览器访问 PhoneBridge 无线传输页面。

如果曾经拒绝，可在“系统设置 → 隐私与安全性 → 本地网络”中重新开启。

## 4. 主界面说明

### 4.1 Mac 文件区

Mac 文件区显示当前本地目录中的文件和文件夹。

支持操作：

- 点击“选择文件夹”切换目录。
- 点击左箭头返回上级目录。
- 点击路径面包屑中的任意层级，直接跳转到该目录。
- 点击刷新按钮重新读取当前目录。
- 输入文件名进行搜索。
- 点击“名称 / 大小 / 日期”切换排序字段；再次点击切换升序和降序。
- 单击文件：使用 macOS 默认应用打开。
- 双击文件夹：进入文件夹。
- 右键文件或文件夹：打开、在 Finder 中显示、拷贝或移到废纸篓。
- 在文件列表空白区域右键：新建文件夹、新建空文件或粘贴 Finder 中已拷贝的项目。

“拷贝”使用 macOS 系统文件剪贴板，可以在 Finder 或 PhoneBridge 目标目录中粘贴。粘贴时若当前目录已有同名项目，会自动生成“`名称 副本`”“`名称 副本 2`”，不会覆盖原项目。

PhoneBridge 会记住最后访问的 Mac 目录，下次启动时自动恢复。如果该目录已被删除，则回到“图片”目录或用户目录。

### 4.2 手机文件面板

每台已连接设备有独立面板，面板中分别保存：

- 当前路径。
- 文件列表。
- 搜索关键词。
- 照片/视频筛选条件。
- 排序方式。
- 已勾选文件。
- 刷新状态。

多台设备连接后：

- 新设备按发现顺序从左到右加入。
- 拖动设备标题栏中的三横线可改变面板顺序。
- 拖动面板之间的分隔线可调整宽度。

### 4.3 投屏侧栏

投屏区启动时默认隐藏。

点击某个设备面板顶部的“投屏”后：

- 该设备成为当前投屏设备。
- 投屏区从最右侧展开。
- 可点击右上角放大按钮进入专注投屏。
- 可点击关闭按钮收起侧栏；已经启动的 iPhone/Android 投屏会继续运行，再点对应设备的“投屏”即可恢复控制。

iPhone 投屏可选择内嵌侧栏或独立窗口；Android 固定使用 scrcpy 独立窗口。投屏工具栏提供截屏、录屏和待保存录像入口。

### 4.4 传输任务栏

传输任务栏显示：

- 等待中、传输中、已完成或失败。
- 单个文件传输百分比。
- 预计剩余时间。
- 当前传输速度，可把鼠标停留在任务上查看。
- 失败任务的“重试”按钮。
- “清除已完成”按钮。

## 5. 连接和浏览 Android

### 5.1 使用 USB 连接

1. 在 Android 中开启“开发者选项”和“USB 调试”。
2. 使用数据线连接 Mac。
3. 在手机弹窗中允许 USB 调试。
4. 在 PhoneBridge 中点击“刷新设备”或按 `Command + R`。
5. Android 面板出现后等待文件列表刷新。

默认目录为：

```text
/sdcard/DCIM
```

双击文件夹可进入下级目录，点击路径栏左侧箭头可返回上级。

点击“照片”或“视频”时，PhoneBridge 不再只筛选当前目录，而是递归扫描以下常用媒体位置：

- `/sdcard/DCIM`
- `/sdcard/Pictures`
- `/sdcard/Movies`
- `/sdcard/Download`

路径栏会显示“`/媒体库/照片（自动扫描）`”或“`/媒体库/视频（自动扫描）`”。点击“全部”可回到普通目录浏览。

为保持界面聚焦媒体传输，Android 面板只显示：

- 文件夹。
- 常见图片格式，例如 JPG、PNG、HEIC、WebP、DNG。
- 常见视频格式，例如 MOV、MP4、MKV、WebM。

其他类型文件不会显示在手机文件列表中。

Android 图片和视频会按需显示缩略图，视频左上角带播放标识。首次显示某个视频时，需要通过 ADB 临时读取文件来抽帧，因此大视频可能较慢；超过 1GB 的视频会跳过缩略图，不影响传输。

### 5.2 使用无线 ADB 连接

1. 保持手机和 Mac 在同一个网络中。
2. 打开 Android“开发者选项 → 无线调试”。
3. 点击“使用配对码配对设备”。
4. 在 PhoneBridge 底部点击“无线连接”。
5. 输入手机显示的配对地址，例如 `192.168.1.20:37123`。
6. 输入手机显示的 6 位配对码，点击“配对”。
7. 返回无线调试主页面，找到连接地址。
8. 在 PhoneBridge 中输入连接地址并点击“连接”。

注意：配对端口和连接端口通常不同，不要直接重复使用同一个端口。

连接成功后，无线 Android 会像 USB 设备一样加入文件面板，文件浏览、传输和 scrcpy 投屏操作不变。

## 6. 连接和浏览 iPhone / iPad

1. 使用数据线连接设备。
2. 解锁 iPhone/iPad。
3. 第一次连接时点击“信任”。
4. 在 PhoneBridge 中点击“刷新设备”或按 `Command + R`。

iOS 面板使用系统 ImageCaptureCore 读取照片库中的可访问媒体，特点如下：

- 以平铺列表显示照片和视频，不展示 iOS 内部 DCIM 目录结构。
- 默认按日期从新到旧排序。
- 日期优先读取文件、EXIF 和系统媒体元数据；元数据缺失时会尝试从常见文件名恢复日期。若仍显示“—”，点击日期表头仍可按设备中的稳定媒体顺序升降序排列。
- 图片显示缩略图。
- 视频缩略图显示播放标识。
- 缩略图按需加载，不会在启动时一次性读取全部原图。

如果 iPhone 使用“优化 iPhone 储存空间”，未下载到手机本地的原件可能不能立即读取。

## 7. 查找、筛选、排序和多选

### 7.1 搜索

在路径栏右侧输入关键词，可按文件名即时筛选。

每台手机的搜索条件相互独立，不会影响其他设备面板。

### 7.2 照片/视频筛选

手机面板支持：

- 全部
- 照片
- 视频

文件夹在 Android 中始终保留显示，便于继续进入下级目录。

### 7.3 排序

点击表头可按以下字段排序：

- 名称
- 大小
- 日期

首次切换字段时使用合理的默认方向，再次点击同一表头可切换升序/降序。

### 7.4 多文件勾选

- 点击文件左侧复选框进行勾选。
- 点击“全选当前结果”可选中当前筛选和搜索结果。
- 再次点击可取消当前结果的选择。
- 已选数量显示在筛选栏旁边。

“全选当前结果”只作用于当前可见结果，不会误选被搜索或类型筛选隐藏的文件。

## 8. 从手机传输到 Mac

### 8.1 勾选后批量传输

1. 在手机面板勾选一个或多个图片/视频。
2. 在 Mac 文件区切换到目标目录。
3. 点击“传输到 Mac（数量）”。
4. 在底部任务栏查看进度、剩余时间和结果。

### 8.2 拖拽传输

支持以下拖拽方式：

- 把单个手机文件拖到 Mac 左侧面板任意位置，保存到当前目录。
- 把多个已勾选文件一起拖到整个 Mac 左侧面板。
- 文件行、列表空白区、顶部路径区和底部提示区属于同一个连续接收区；快速移动时不需要瞄准窄小热区。
- 把 Mac 文件拖到 Android 面板，保存到该面板当前目录（媒体自动扫描页默认保存到 `/sdcard/Download/PhoneBridge`）。
- 把 Mac 文件拖到 iPhone 面板，打开一次性下载页；在 iPhone 下载后选择“存储到文件”。

如果开始拖动的文件已经被勾选，则一起拖动当前已选文件；否则只拖动当前文件。

### 8.3 同名文件处理

目标目录中存在同名文件时，PhoneBridge 会提示选择：

- 跳过：保留 Mac 原文件，不传输冲突文件。
- 自动重命名：保留两份，新文件命名为 `文件名 (1).扩展名` 等形式。
- 覆盖：传输完成后替换 Mac 原文件。

建议一般使用“自动重命名”。覆盖并不是先删除原文件，而是先下载到临时文件，成功后再执行替换，降低中途失败破坏原文件的风险。

### 8.4 传输后的日期

手机文件传到 Mac 并完成落盘后，PhoneBridge 会把 Finder 使用的“修改日期”更新为本次传输完成时间。因此可以点击 Mac 文件区的“日期”表头，快速找到刚接收的文件。

该处理适用于：

- Android USB / 无线 ADB 传到 Mac。
- iPhone / iPad USB 传到 Mac。
- 手机浏览器无线上传到 Mac。

只改文件系统的修改日期，不会更改照片 EXIF 拍摄时间或视频内部的创建时间。

## 9. 从 Mac 传输到手机

### 9.1 传输到 Android

1. 在目标 Android 面板顶部点击“传输到手机”。
2. 在文件选择器中选择一个或多个 Mac 文件。
3. 在文件窗口下方的“手机目标文件夹”中选择预设目录，或输入以 `/` 开头的完整 Android 路径。
4. 点击“选择并传输”。
5. 在任务栏直接查看每个任务的完整手机目标位置、进度和剩余时间。

目标目录不存在时，PhoneBridge 会自动创建。也可以从 Mac 文件区把文件直接拖到 Android 面板，拖拽默认使用当前正在浏览的手机目录。

如果任务失败，可在任务栏点击“重试”。

### 9.2 传输到 iPhone

1. 在 iPhone 面板点击“传输到手机”，或直接把 Mac 文件拖到 iPhone 面板。
2. 选择一个或多个文件后，PhoneBridge 显示一次性二维码下载页。
3. iPhone 扫码并逐个下载文件。
4. 在 iOS 下载/分享菜单中选择“存储到文件”，再选择 iCloud Drive、在我的 iPhone 上或具体 App 文件夹。

iOS 公开接口不允许 Mac 预先指定或静默写入任意手机文件夹，所以目标位置必须在 iPhone 端确认。

## 10. 发送文本或网站链接到手机

每台手机面板顶部都有“文本”按钮，可发送网站链接、账号、验证码说明或多行文本。

操作步骤：

1. 在目标手机面板点击“文本”。
2. 输入内容，或点击“粘贴 Mac 剪贴板”。
3. 点击“生成手机页面”。
4. Android USB/ADB 连接会通过 `adb reverse` 自动在手机浏览器打开；即使不在同一 Wi-Fi，也可通过数据线访问。
5. iPhone 使用相机扫描二维码打开，因此需要能访问 Mac 的局域网地址。
6. 在手机页面选择“复制文本”“分享到备忘录 / 其他 App”；如果整段内容是 HTTP/HTTPS 地址，还可选择“打开链接”。

受 Android/iOS 浏览器安全规则限制，PhoneBridge 不能在用户无操作时静默改写手机剪贴板或直接创建备忘录；必须在手机页面点一次复制或分享。内容只由 Mac 本地临时页面提供，不上传云端，关闭文本窗口后页面和令牌立即失效。

## 11. 向 iPhone 发送 Charles CA 证书

iOS 的系统照片接口不允许 PhoneBridge 通过 USB 任意写入文件，因此 Charles 证书使用二维码网页下载。

支持文件类型：

- `.cer`
- `.crt`
- `.pem`
- `.mobileconfig`

操作步骤：

1. 保持 iPhone 和 Mac 在同一个局域网或热点中。
2. 在 iPhone 面板顶部点击“传输到手机”，选择 Charles 证书文件。
3. 在 Mac 上选择证书文件。
4. PhoneBridge 显示二维码和下载地址。
5. 使用 iPhone 相机或扫码工具打开二维码。
6. 在网页中下载证书。
7. 打开“设置 → 通用 → VPN 与设备管理”，安装描述文件。
8. 打开“设置 → 通用 → 关于本机 → 证书信任设置”，开启完全信任。

如果 iPhone 已经把 HTTP 代理指向 Charles，也可以在 Safari 打开：

```text
http://chls.pro/ssl
```

二维码地址包含一次性随机令牌。关闭 PhoneBridge 的无线传输窗口后，本地服务停止，旧链接立即失效。

## 12. 手机无线上传文件到 Mac

该功能适用于没有数据线、但手机和 Mac 位于同一局域网或热点的情况。

1. 先在 Mac 文件区进入希望接收文件的目标目录。
2. 点击 PhoneBridge 底部的“无线传输”。
3. 使用手机扫描二维码。
4. 在手机浏览器中选择一个或多个文件。
5. 点击“开始上传”。
6. 上传完成后，文件出现在当前 Mac 目录中。

如果目录中已有同名文件，无线接收会自动生成 `文件名 (1)`、`文件名 (2)`，不会覆盖原文件。

注意：

- 无线传输使用本地 HTTP，不经过云端服务器。
- 手机必须能直接访问二维码中的 Mac 局域网 IP。
- VPN、代理、防火墙、访客 Wi-Fi隔离或公司网络策略可能阻止访问。
- 关闭无线传输窗口后服务停止。
- 底部“无线传输”入口当前主要用于手机上传到 Mac；Mac 文件下载入口主要用于“Charles 证书”共享场景。

## 13. Android 投屏

1. 通过 USB 或无线 ADB 连接 Android。
2. 在对应设备面板点击“投屏”，展开最右侧投屏栏。
3. 点击“打开独立窗口”。

Android 投屏支持：

- USB ADB。
- 无线 ADB。
- scrcpy 独立窗口：保留低延迟、键鼠控制、剪贴板、拖放和保持唤醒能力。
- 多台 Android 分别打开 scrcpy 窗口。

Android 不再提供内嵌模式。若独立窗口断开，请检查 USB 调试授权、数据线或无线 ADB 连接后重新打开。只有使用 PhoneBridge 的截屏或录屏功能时，才需要授予“屏幕与系统录音”权限。

## 14. iPhone 投屏

### 14.1 自定义接收名称

多人在同一空间使用 PhoneBridge 时，建议先设置容易辨认的 AirPlay 名称：

1. 点击底部“无线连接”。
2. 最上方就是“iPhone AirPlay 投屏（名称与启动）”，无需先插数据线或发现 USB iPhone。
3. 在名称框中输入名称，例如 `PhoneBridge · 张三`、`PB-工位12`。
4. 点击“保存名称”或按回车。
5. iPhone 的“屏幕镜像”列表以及 PhoneBridge 投屏侧栏会显示这个名称。

名称支持中文，最长 48 个 UTF-8 字节，会自动合并首尾和连续空白；留空时恢复为 `PhoneBridge`。设置会在退出后保留。如果投屏正在运行，应用名称后接收器会自动重启，需要在 iPhone 上重新选择一次。

通过某台已连接 iPhone 的设备面板启动投屏时，实际 AirPlay 名称会自动变成“基础名称 · 设备名”；多台设备重名时再附加序号。这样可在 iPhone 控制中心准确选择对应接收器。无线连接页直接启动、且未指定 USB iPhone 时，仍使用基础名称。

### 14.2 普通局域网投屏

1. 保持 Mac 与 iPhone 在同一个局域网。
2. 打开“无线连接”，选择“内嵌显示”或“独立窗口”。
3. 点击“启动 iPhone 接收器”；没有连接数据线也可以启动。
4. 打开 iPhone 控制中心。
5. 点击“屏幕镜像”。
6. 选择 PhoneBridge 界面显示的接收名称。

- 内嵌显示：画面进入最右侧投屏栏，可使用专注放大。
- 独立窗口：UxPlay 把画面交给 PhoneBridge 原生独立窗口显示，不占用文件面板；若手动关闭，可点击侧栏中的“显示独立窗口”重新打开。

### 14.3 附近设备模式

普通网络无法发现时，可尝试：

1. 点击底部“无线连接”。
2. 开启“没有共同 Wi-Fi 时，使用附近设备投屏（AWDL）”。
3. 记住界面显示的 4 位 PIN。
4. 直接点击“启动 iPhone 接收器”。
5. 在 iPhone“屏幕镜像”中选择界面显示的接收名称。
6. 按提示输入 PIN。

附近模式要求 Mac 和 iPhone 的 Wi-Fi、蓝牙保持开启。能否成功还取决于机型、系统版本和现场无线环境。

### 14.4 画质档位

| 档位 | 目标分辨率 | 最大帧率 | 适合场景 |
| --- | --- | --- | --- |
| 清晰优先 | 1920×1080 | 30 fps | 查看文字、界面细节 |
| 流畅优先 | 1920×1080 | 60 fps | 操作演示、动态画面 |
| 节省带宽 | 1280×720 | 30 fps | 无线环境较差、降低资源占用 |

右侧栏会显示实际收到的分辨率和帧率。实际清晰度受 iPhone 输出、网络质量和 Mac 性能影响，不一定始终达到目标值。

### 14.5 截屏与录屏

投屏画面出现后，使用侧栏底部工具栏：

1. 点击“截屏”，选择 PNG 文件名和保存位置。
2. 点击“开始录屏”，工具栏显示红点和已录制时长。
3. 点击“停止并命名保存”，等待 MP4 生成完成。
4. 在弹出的“另存为”窗口中修改录像名称并选择保存位置。

录屏文件使用 H.264 MP4 格式，最长边限制为 1920 像素。默认文件名包含录制结束时间，也可以自由修改；“另存为”窗口会自动打开上次保存截图或录像的位置。若取消保存，本次录像仍会临时保留，可点击“保存录像”重新命名和选择位置。

iPhone 的内嵌和独立窗口都直接使用 AirPlay 投屏帧，不额外申请屏幕录制权限。Android 独立窗口本身不需要 PhoneBridge 的屏幕录制权限；只有使用截屏和录屏时，PhoneBridge 才通过 ScreenCaptureKit 采集 scrcpy 窗口，需要在“系统设置 → 隐私与安全性 → 屏幕与系统录音”中获得授权。首次授权后需完全退出并重新打开应用。

当前 iPhone 投屏只显示视频画面，不接收音频。

## 15. 多设备使用

PhoneBridge 可以同时显示多台 Android 和 iPhone/iPad 的文件面板。

支持：

- 按连接顺序加入设备。
- 独立路径、列表、筛选、搜索、排序和勾选状态。
- 拖动标题栏三横线调整设备顺序。
- 同时为多台 Android 打开 scrcpy 窗口。
- 同时为多台 iPhone 启动独立的 AirPlay 接收器；每台设备拥有独立画面端口和窗口。
- 同时混合投屏 Android 与 iPhone；点击不同设备的“投屏”可在侧栏切换控制对象，不会停止其他设备。
- iPhone 选择“独立窗口”时可同时看到多路画面；选择“内嵌显示”时，多路投屏保持运行，侧栏显示当前选中的一路。

限制：

- 同一时间只能对当前侧栏选中的一路投屏执行截屏或录屏；结束后可切换到另一台设备。
- 多路 iPhone 高清投屏会分别进行 JPEG 编解码，CPU、内存和网络带宽占用会随路数增加。
- 多设备完整体验需要足够宽的窗口；可拖动分隔线调整面板宽度。
- USB 口、电源、线材和系统设备服务可能限制实际可稳定连接的设备数量。

## 16. 常见问题排查

### 16.1 iPhone 已连接但没有文件

按顺序检查：

1. iPhone 是否解锁。
2. 是否点击“信任此电脑”。
3. 是否使用可传数据的数据线。
4. 点击 PhoneBridge“刷新设备”。
5. 关闭并重新打开 iPhone 照片访问会话：拔线、重新连接、再次信任。
6. 原片是否只在 iCloud 中，尚未下载到设备。

### 16.2 Android 面板没有出现

检查：

1. USB 调试是否开启。
2. 手机是否允许这台 Mac 调试。
3. USB 连接模式和线材是否支持数据。
4. 是否有其他 ADB 工具占用或重启了 ADB 服务。
5. 无线模式下是否使用了正确的连接端口。

### 16.3 文件传输失败

检查：

- 手机是否中途锁屏、断线或撤销授权。
- Mac 目标目录是否仍存在且可写。
- Mac 磁盘空间是否足够。
- 手机中的文件是否已被删除或移动。
- 失败后点击任务右侧“重试”。

PhoneBridge 使用 `.phonebridge-*.part` 临时文件。失败时会清理临时文件，不会把未完成文件当成成功结果。

### 16.4 iPhone 找不到 PhoneBridge 投屏

检查：

1. Mac 是否允许 PhoneBridge 访问本地网络。
2. 是否已经在“无线连接”中点击“启动 iPhone 接收器”；不插数据线时不会自动启动。
3. Mac 与 iPhone 是否处于同一 Wi-Fi。
4. 公司 Wi-Fi 是否隔离终端或屏蔽 Bonjour/mDNS。
5. VPN 或代理是否阻断局域网地址。
6. 没有共同 Wi-Fi 时启用附近设备模式，并保持 Wi-Fi、蓝牙开启。
7. 确认 iPhone 上选择的是当前自定义接收名称；改名后需要重新连接。

### 16.5 iPhone 投屏模糊或卡顿

- 文字模糊：选择“清晰优先”。
- 动态卡顿：选择“流畅优先”。
- 网络不稳定：选择“节省带宽”。
- 尽量缩短手机与 Mac 的距离。
- 避免同时进行大量无线文件传输。
- 观察侧栏显示的实际分辨率和 fps，区分源端分辨率不足与网络丢帧。

### 16.6 无线传输二维码打不开

检查：

- 手机与 Mac 是否在同一网络或同一个热点。
- 二维码中的 IP 是否是手机可访问的局域网地址。
- 是否关闭了无线传输窗口；关闭后旧链接失效。
- Mac 防火墙、VPN、Charles 或其他代理是否拦截了本地地址。
- 访客 Wi-Fi 是否开启了客户端隔离。

## 17. 当前能力边界

- iPhone 文件面板只读取系统允许访问的照片和视频。
- iPhone 不支持通过当前 USB 接口任意写入文件。
- Mac→iPhone 支持通过一次性页面下载普通文件和 Charles 证书，但保存位置必须由用户在 iOS 端选择，不等同于完整 iOS 文件管理器。
- Android 文件面板只显示目录、图片和视频。
- 手机文件暂不能直接拖到 Finder，只能拖到 PhoneBridge 的 Mac 文件区。
- Android 投屏固定使用 scrcpy 独立窗口，支持键鼠控制、剪贴板和拖放。
- iPhone 内嵌投屏使用 JPEG 帧流，不是 H.264/VideoToolbox 直解码，因此高帧率时会有额外 CPU 与带宽开销。
- iPhone 投屏没有音频。
- 附近设备 AirPlay 和 Android 无线配对需要真实设备、系统版本和现场网络共同支持。

## 18. 退出与安全建议

- 传输完成后再断开手机，避免任务失败。
- 不使用无线传输时关闭二维码窗口，使随机链接立即失效。
- Charles CA 属于高权限证书，只在受信任设备和网络中分发。
- 不再抓包时应关闭代理，并按需要取消证书完全信任或删除描述文件。
- 删除 Mac 文件时 PhoneBridge 使用系统废纸篓，可以从废纸篓恢复。
